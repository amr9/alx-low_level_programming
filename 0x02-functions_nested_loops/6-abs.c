#include "main.h"

/**
 * _abs - check the code
 * Description : gets the absolute value
 * @c : parameter for number
 * Return: new absolute value.
 */
int _abs(int c)
{
	if (c < 0)
	{
		return (c * -1);
	}
	else
	{
		return (c);
	}
}
